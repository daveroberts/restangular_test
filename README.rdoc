== README

To get this up and running:

* Clone the repo

* Run bundle

* bundle exec rake db:migrate

* bundle exec rake db:seed

* rails s

* Navigate to localhost:3000

* Edit the form, hit save, observer the network traffic sent
